# 验证结果

验证日期：2026-09-12。最终代码：`600565c2f4947bb2a16b0dd025d7a9918d32ac7a`，Tag：`rhino-2026-final-4`。原型覆盖课题四要求的设计、可运行功能、有效性验证和个人数据隔离；下述结论仅适用于明确列出的测试范围。

## 工程验证

| 项目 | 结果 |
| --- | --- |
| Go 全量测试 | 通过；合入上游后执行 `go test -p 8 -count=1 ./...`，最终提交再次执行全量 |
| Go vet | 通过 |
| PostgreSQL / SQLite | 学习仓储、来源变更/删除、幂等计分、Wiki rename 和核心服务 `-race` 通过 |
| 新空库迁移 | PostgreSQL `94`、clean；五张学习表和三张浏览器表齐全；SQLite 真实目录迁移测试到 `15` |
| Go lint | CI 同版 `v2.12.2`，相对上游 `081851c6` 增量 0 issues；保留主线已有 unknown-nolint 警告 |
| 前端单元测试 | 938 项通过，0 失败、0 跳过 |
| 前端类型 / 生产构建 | 通过；构建有现有大 chunk 警告 |
| 夹具离线测试 | Python 53 项、Node 配置 63 项通过 |
| 文档 | 65 篇、207 内部链接无失效；VitePress 构建通过 |

本次补充真实 memory 仓储到候选与推荐的集成测试，以及 Wiki 重命名后学习记录/卡片保留测试。修复跨卡片隐私操作等待状态、首个响应前暂停后的恢复入口，补齐普通 checkout 可用的夹具初始化脚本。上游浏览器功能占用旧迁移编号后，学习迁移重新编号，SQL 内容未变。

## 真实服务

最终验收使用新建 PostgreSQL 数据库和独立 Redis 命名空间，初始用户、文档为 0。通过公开 API 创建两个一次性用户及独立租户、六份合成来源文档和六个带实际 chunk 引用的 Wiki 页面。来源均完成真实解析；重复 seed 后仍为两用户、六文档。

环境：Go 1.26.0、Node 22.16.0、Python 3.11.2、PostgreSQL 17、Redis 7、Qdrant 1.18.1、Chromium 153.0.8010.12。Node 实测版本与仓库 CI 的 Node 24 不同。模型使用 DeepSeek `deepseek-v4-flash`；没有用模型 mock 替代真实出题或 Agent 调用。

真实 API **36 项通过**，15 项观测单独计数，覆盖：

- 默认关闭、显式开启、概览、稳定推荐、阅读只改变熟悉度、Wiki overlay。
- 三题异步生成、未作答时不泄露答案、引文来自有效来源。
- 固定选择第一个选项，由服务端判分；BKT 更新、重放幂等、冲突返回 409。
- 另一租户读取和提交答案返回 404。
- 内置学习 Agent 实际调用概览、推荐、Wiki 阅读和测验准备，工具结果只保存卡片引用。
- 关闭后仍可导出和清除；非空记录清空、重复清空幂等、另一账号历史保留。

最终提交重建并重启后再次执行 API 验收，36 项仍通过，第一账号已有 3 次答案、1 份题集未丢失。

## 浏览器

桌面 `1280×900`、移动端 `390×844` 的最终全流程重放：**11 项通过，0 失败，3 项跳过**。运行期间源码未变化，未使用单进程兼容模式，未发现未预期 HTTP/console/page 错误。

首次实跑新生成题集，通过界面提交三题、核对正误与当前 chunk 引文、刷新恢复答案、创建真实 Agent 卡片。第一次卡片展开测试误选含工具名称的思考文本；修正定位器后，同一卡片重放通过，随后完整重放通过。最终重放没有重复生成或计分。

独立非空删除流程 **2 项通过、0 跳过**：另一账号先完成三题，再通过界面导出、确认并删除。删除结果为 `3 attempts / 1 mastery / 1 quiz`，另一账号导出摘要完全不变。

三项跳过不计为成功：

- 来源不足分支：当前来源均有效，未人为破坏来源；确定性后端测试覆盖缺失/越权/过期证据。
- 重复导出：由已答题集的导出流程覆盖，避免重复触发导出限流。
- 非空删除：在单独两项流程执行，未混入重放。

11 项通过中包含一个明确标记的 **mock 503 错误恢复**用例，仅此用例模拟了响应；恢复后读取真实后端。

## 合成评估

固定数据集 `topic4-synthetic-v1`：8 个推荐场景、12 个主题、64 次候选出现、9 个状态快照；8 条答题序列、21 次训练观测、24 次留出观测。数据 SHA-256：`8bc5d4d5fe393466ba5abd27f74f4c47301a887f88e8d6170d376b6cf3f43237`。

| 推荐方法 | NDCG@5 | Recall@5 |
| --- | ---: | ---: |
| 当前推荐 | 0.813841 | 0.777827 |
| 仅节点度数 | 0.554398 | 0.537054 |
| 冷启动 | 0.595027 | 0.588244 |

| 留出预测 | Brier，越低越好 | LogLoss，越低越好 |
| --- | ---: | ---: |
| BKT | 0.277601 | 0.780492 |
| 固定猜测 0.25 | 0.312500 | 0.836988 |
| 固定先验响应 0.38 | 0.264400 | 0.722810 |

BKT 优于固定猜测，弱于固定先验。推荐的合成排序结果不能推出真人学习收益；数据集规模小且为人工构造，没有真人试用、随机对照、P95 或负载测量。

## 保留边界

同模型盲答不是独立评审，不能保证每道题正确。实现未建立课程先修推理，推荐是有上限候选中的启发式排序。任务恢复扫描和 Wiki 重命名锁范围尚未进行大库负载优化。跨浏览器标签页不提供即时广播，重新聚焦或请求时重新检查权限。

从远程最终 Tag 克隆到独立目录，53 项 Python 测试、63 项配置测试和离线评估通过。初次将克隆嵌套到另一 checkout 的 `.runtime/` 时，路径配置测试被拒绝；当前不支持该目录布局，运行说明已明确要求独立目录。

截图仅含合成来源和一次性测试数据，登录身份区域已遮盖。提交包不含账号文件、密钥、完整数据库、trace/HAR 或原始服务日志；[机器可读证据](evidence/verification.json)与[离线输出](evidence/offline-evaluation.txt)可复核上述结论。
